---
apiVersion: v1
kind: Service
metadata:
  name: suzieq
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-internal: "true"
  labels:
    app: suzieq
spec:
  ports:
  - port: 8000
    name: gnmi
  type: LoadBalancer
  selector:
    app: suzieq